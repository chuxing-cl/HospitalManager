<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--  使用mapper接口
  namespace : 与mapper接口同包同名
-->
<mapper namespace="com.czy.mapper.AdminsMapper">

    <!-- 根据用户名查询用户信息-->
    <select id="selectAdminByName" resultType="com.czy.pojo.Admins">
        select * from admins
        where username = #{username}
    </select>

</mapper>